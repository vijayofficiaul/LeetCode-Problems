// Problem 455. Assign Cookies
// Link: https://leetcode.com/problems/assign-cookies?envType=problem-list-v2&envId=vmgzc3ut

class Solution {
    public int findContentChildren(int[] g, int[] s) {

        Arrays.sort(g);
        Arrays.sort(s);

        int i = 0;
        int j = 0;
        int count = 0;
        while (i<g.length && j<s.length){
            if(s[j] >= g[i]){

                count ++;
                i++;
                j++;

            }else if(s[j] < g[i]){
                j++;
            }
        }

        return count;
    }
}
